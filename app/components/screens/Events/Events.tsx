import Meta from "@/core/utils/Meta";
import { FC } from "react";

const Events: FC = () => {
    return (
        <Meta title="Новости">
            <div></div>
        </Meta>
    );
};

export default Events;
